package Interfaces;

public interface Convertable {
    
        /**
         * Converts the object into a byte.
         * @return must return that byte.
         */
        byte[] toByte();
        
}
